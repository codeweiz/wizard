const urls = {

    // config.json 地址
    configJson: '/wizard/config.json',

    // 登陆相关
    system: {
        user: {
            login: 'api/auth/auth/login',
            register: 'api/auth/auth/register'
        }
    }
}

export default urls;
