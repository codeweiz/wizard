const urls = {

    // config.json 地址
    configJson: '/wizard/config.json',

    // 登陆相关
    system: {
        user: {
            login: 'api/auth/auth/login'
        },
        test: {
            test1: 'api/system/test/test1'
        }
    }
}

export default urls;
